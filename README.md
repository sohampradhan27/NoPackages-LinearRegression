# NoPackages-LinearRegression
A hands-on Python demo that generates noisy linear data, normalizes it, and uses gradient descent to learn the best-fit line—complete with live plotting and progress updates.
